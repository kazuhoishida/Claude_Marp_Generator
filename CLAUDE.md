# CLAUDE.md

Marp によるスライド生成システム。ラフな原稿を入力として受け取り、
体裁の整ったスライド（markdown → PDF / PPTX）を出力する。

このファイルに、作業の流れと、体裁・中身のルールをすべて置いている。
**スライドを作る前に最後まで読む。**

## ディレクトリ

```
.claude/commands/      スラッシュコマンド（/create-slides）
.vscode/settings.json  VS Code の Marp 拡張にテーマを教える設定
tools/                 素材の準備と pptx の後処理
themes/theme.css       カスタムテーマ。見た目の定義はすべてここ
.images/               テンプレートで使うプレースホルダー画像
input/                 入力（ラフな原稿・議事録・箇条書きメモ）
output/                出力（.md / .pdf / .pptx と素材）
resources/             参照用の過去記事・ブログ・既存資料
YYYYMMDD_template.md   スライドのテンプレート兼・記法の見本
```

`input/` `output/` `resources/` の中身は git で追跡しない。
このリポジトリは「仕組みと見た目」のマスターとして扱う。

## 基本の流れ

1. `input/` の原稿を読む。無ければユーザーに元ネタを聞く
2. `resources/` に関連資料があれば目を通す
3. 写真・動画が付いてくるなら `tools/` で整える（後述）
4. `YYYYMMDD_template.md` を下敷きに `output/<日付>_<名前>.md` を書く
5. **png に書き出して目視で確認する**（後述。省略しない）
6. 問題があれば直して再確認。通ってから PDF / PPTX にする

## ビルド

`marp` は devDependency。初回だけ `npm install` する。

```bash
npm install                              # 初回のみ
npm run images -- output/foo.md          # png で書き出して目視確認
npm run pdf    -- output/foo.md          # PDF を出力
npm run pptx   -- output/foo.md          # PowerPoint（各ページが画像。編集不可）
npm run pptx-editable -- output/foo.md   # 文字を編集できる PowerPoint
npm run preview                          # ブラウザでライブプレビュー
npm run assets                           # プレースホルダー画像を再生成
```

`--theme-set themes/` と `--allow-local-files` は npm script に入れてある。
`marp` を直接叩くときは自分で付ける。付け忘れるとテーマが当たらない、
またはローカル画像が読み込まれない。

`pptx-editable` は LibreOffice が要る（`brew install --cask libreoffice`）。
PDF 経由で変換するため実験的な機能で、変換後は `tools/embed_videos.py` で
体裁を整える前提。

## 素材の準備

原稿の写真・動画はそのまま使わない。`tools/` で整えてから貼る。
ffmpeg が要る（`brew install ffmpeg`）。

```bash
python3 tools/prepare_images.py <出力先> <画像...>       # 長辺1400pxに縮小
tools/convert_video.sh <in.mov> <out.mp4> [poster.png]   # 720p mp4 + 静止画
python3 tools/embed_videos.py <pptx>                     # pptx の後処理
```

動画は PDF に入らない。スライドには静止画を置き、PPTX にだけ埋める。
どのスライドにどの動画を入れるかは素材ディレクトリの `videos.json` に書く。
`embed_videos.py` は動画の埋め込みに加えて、PDF 経由の変換で崩れた
テキストボックス（1行ごとの分割・左右非対称な幅・白い全面シェイプ）も直す。
`python-pptx` が要る（`pip install python-pptx`）。

## 確認を省略しない

**markdown を書いただけで「できました」と報告しない。**
CSS の効き方はレンダリングしないと分からない。

```bash
npm run images -- output/foo.md
```

書き出した png を Read ツールで実際に見る。少なくとも
**画像を含むスライド全部**と**コードブロックを含むスライド全部**は見る。

確認する観点:

1. 画像がスライドの上下端・左右端に触れていないか / はみ出していないか
2. コードの文字が背景に埋もれていないか
3. 本文が下端からはみ出していないか
4. 1枚に詰め込みすぎていないか

はみ出しを見つけたら、フォントサイズを小さくして押し込むのではなく
**スライドを分割して直す**。確認用の png は最後に消す。

## 入力に無いことを書かない

原稿に無い数値・パス・チャンネル名・製品名を推測で補完しない。
埋めるべき箇所は `TODO:` を残してユーザーに聞く。
スライドは後から参照される資料なので、もっともらしい嘘が一番害になる。

---

# 中身のルール

構成・見出し・文章の作り方。

## 最初に決めること

書き始める前に、この3つを言語化する。曖昧なまま書くと、
情報は正しいのに何も伝わらないスライドになる。

1. **聞き手は誰か** — 前提知識はどこまであるか
2. **聞き手に何をしてほしいか** — 理解？判断？作業？
3. **一番伝えたいことは何か** — 1文で言えるか

入力にこれが書かれていない場合は、原稿から推測して
出力の冒頭にコメントで残す。推測が外れていれば指摘してもらえる。

## 見出しは主張にする

見出しはラベルではなく、そのスライドで言いたいことそのものにする。
見出しだけを拾い読みして話の筋が通るなら、構成は正しい。

- 悪い: `パフォーマンスについて`
- 良い: `描画をブロックしないため別プロセスで動かす`

ただし手順書・マニュアルのように「引く」ことが目的の資料では、
探しやすさを優先してラベル型の見出し（`セットアップ手順` など）でよい。
**資料の目的によって使い分ける。**

## 構成の型

**説明・提案の資料**

1. 表紙
2. 課題 — なぜこの話が必要か
3. 結論 — 先に言う
4. 根拠・詳細 — 結論を支える材料
5. 次のアクション

**手順書・マニュアル**

1. 表紙
2. 全体像 — 何と何があるか。ここで地図を渡す
3. 各パートの概要 → 動作 → 詳細手順 → 補足（トラブル対応）
4. 困ったときの参照先

どちらも、章の切り替わりには `section-divider` の扉を挟む。
聞き手が「いま何の話か」を見失わないため。

## 文章

- 1スライドの本文は箇条書き中心。段落で書かない。
- 体言止めと敬体を混ぜない。手順書なら常体（〜する）で統一する。
- 1文は40文字前後で切る。長くなったら改行ではなく句点で切る
  （markdown 側で改行しない理由は「スライドの区切り」を見る）。
- 修飾語を削る。「非常に」「しっかりと」「〜という形で」は消してよい。
- 具体的な値・パス・コマンドは省略せずに書く。
  スライドは後から参照される。「適宜設定する」では読み手が困る。

## 数字と固有名詞

- 数字には単位と根拠を付ける。`5秒`ではなく`デフォルト5秒`のように、
  変更可能なのか固定値なのかが分かるようにする。
- 設定値はファイルパスと変数名をセットで書く。
  例: 待機時間: `NEW_FILE_SETTLE_SECONDS`（`src/scanner.py`）
- 外部サービス名・チャンネル名は正確に。うろ覚えで書かない。

## 図の入れどころ

図は「文章で説明すると長くなるもの」に使う。装飾で入れない。

- 画面のスクリーンショット → 操作手順の説明
- 構成図 → 登場人物が3つ以上あるとき
- ログ・実行結果 → 「成功した状態」を示すとき

図を入れるスライドは、**説明のスライドと図のスライドに分ける**（「画像」の節）。
図には必ず、何を見てほしいのかを1行添える。

## 削る

作り終えたら、各スライドについて「これを消したら何が伝わらなくなるか」を問う。
答えられないスライドは消す。枚数は成果ではない。

---

# 体裁のルール

記法・レイアウト・配色・画像。ここに書いてあるのは
「見た目が破綻しないための制約」で、守らないと文字が読めない・
画像がはみ出すといった形で必ず表面化する。

## front-matter

出力する markdown の先頭は必ずこの形にする。

```yaml
---
marp: true
theme: custom-deck
paginate: true
header: "デッキのタイトル"
---
```

- `theme` は必ず `custom-deck`。ビルド時に `--theme-set themes/` を渡す前提。
- front-matter に `style:` を書いて色やサイズを上書きしない。
  見た目の調整は `themes/theme.css` に集約する。1枚だけ例外的に効かせたい場合のみ、
  そのスライドの先頭に `<!-- _class: ... -->` を書く。
- 表紙にはページ番号を出さない → `<!-- _paginate: false -->`

## スライドの区切り

- `---` で区切る。front-matter の直後の `---` は区切りとして数えられないので、
  1枚目の本文はそのまま書き始める。
- **1スライド1メッセージ**。見出しが2つ以上の話題を含んでいたら分割する。
- 1枚に詰め込む本文は、箇条書きで **6行以内** を目安にする。
  超えるなら「前提」と「詳細」でスライドを割る。
- **文中で改行しない。** 長い文もそのまま1行で書けば、スライドの幅で
  自動的に折り返る。手動で改行すると、PPTX に変換したとき1行ごとに別の
  テキストボックスに分かれ、あとから文言を直しても文章が流れ直さなくなる。

## 使えるレイアウト（class）

`<!-- _class: name -->` をスライド先頭に書いて指定する。

| class | 用途 |
| --- | --- |
| `title` | 表紙。中央寄せ |
| `lead` | 中央寄せ。章扉・締めのスライド |
| `section-divider` | 章扉。スライド全面が薄いグレーになり、見出しに白い下線が入る |
| `figure-full` | 図版が主役のスライド。画像を大きく表示する |

## 使える部品（div）

class ではなく HTML で書く。section 側のクラス指定は不要。

| class | 用途 |
| --- | --- |
| `columns` | 2カラム。対比させる |
| `columns-3` | 3カラム。短い語句を並べる |
| `cards` | 並列の項目をグレーの面に載せる。2〜3個まで |
| `stats` | 数値を大きく見せる。2〜4個まで |
| `steps` | 手順。番号は自動で振られる |
| `callout` | 一番言いたい1文を囲む。**1スライドに1つまで** |

見出しの上に小さなラベルを置きたいときは `<p class="label">ラベル</p>` を使う。

```html
<div class="cards">
<div>

### 見出し

本文

</div>
<div>

### 見出し

本文

</div>
</div>
```

`<div>` と中身の間の**空行は必須**。空行が無いと markdown として解釈されず、
箇条書きや強調がそのまま文字列で出る。

`stats` は強調した数字が大きく出る。数字の下の行はその数字の説明にする。

```html
<div class="stats">
<div>

**3秒**

処理時間（変更前は12秒）

</div>
</div>
```

部品を1枚に2種類以上入れない。カードと数値を同居させると、
どちらが主役か分からなくなる。

## 画像

**画像をスライドの端に接触させない。** これが一番よく壊れるところ。

- `![bg](...)` の背景画像は使わない。表紙も白地に文字だけで組む。
- `![bg right:45%](...)` のような分割背景も使わない。
  画像がスライド端まで到達して余白が消えるうえ、縦長画像だと下端からはみ出す。
- 画像に影・角丸・枠は付けない。テーマ側でも付けていない。
- 本文中の画像は素の `![](path)` で置く。サイズはテーマ側で上限が決めてあるので、
  `![w:600]` のような幅指定は原則書かない。
- **テキストと画像を同じスライドに詰め込まない。**
  本文が3行以上あるスライドに画像を足すと、ほぼ確実に下端からはみ出す。
  「説明のスライド」→「図のスライド（`figure-full`）」と2枚に割る。
  図のスライドには説明スライドと同じ見出しを付けて対応を示す。
- 画像パスは markdown ファイルからの相対パスで書く。
  `output/` に置く markdown からは `assets/<デッキ名>/foo.png` のようになる。

## 配色とコード

配色は `themes/theme.css` が持っている。markdown 側で色を指定しない。

- 背景は白。テーマを変えて濃色背景にしない（印刷とプロジェクタで破綻する）。
- 見出しは本文と同じ黒。色で目立たせない。
- アクセント色を使うのは1スライドに1〜2箇所まで。
  カードの罫、数値、手順の番号のどれか1つに絞る。
- **絵文字は使わない。**
- インラインコード `` `foo` `` は薄グレー背景 + 赤系文字。
- コードブロックは薄グレー背景 + 濃いグレー文字。
  言語名を必ず付ける（` ```python ` など）。付けないとハイライトが効かず、
  地の文と区別が付かなくなる。
- コードブロックは **12行以内**。超えるなら該当箇所だけ抜き出す。

## やらないこと

- front-matter の `style:` での色・サイズ上書き（テーマに書く）
- `![bg]` の背景画像
- 画像の幅・高さの個別指定でのつじつま合わせ
- 文字サイズを下げてスライドに押し込むこと
- 1スライドに2つ以上の主張を載せること
- 1スライドに部品（cards / stats / steps / callout）を2種類以上置くこと
- 絵文字での装飾
- 文中での手動改行（PPTX で編集できなくなる）
